services:
  - type: web
    name: studybuddy
    env: python
    region: ohio
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: ENABLE_RAG
        value: "false"
